/target
guest/target
# Materialized by guest/setup.sh from the checked-in `Cargo.toml.dist` copies — see that
# script for why the guest workspace can't check in real `Cargo.toml` files.
guest/Cargo.toml
guest/cargo-unikernel-common/Cargo.toml
guest/cargo-unikernel-init/Cargo.toml
/linux-*
/rootfs
linux.tar.xz
sev_measurement.txt
usr/
sev-snp-measure/
dist/
*.bzImage
*.efi
*.iso
.claude