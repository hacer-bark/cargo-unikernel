[workspace]
resolver = "2"
members = ["cargo-unikernel-common", "cargo-unikernel-init"]

[workspace.package]
version = "0.0.1"
edition = "2024"
license = "MIT OR Apache-2.0"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
