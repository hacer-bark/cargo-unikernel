# Applied when `[network].mode` is "ipv6" or "dual" — virtio-net plus IPv6 support;
# addressing is via SLAAC, so no cmdline parameter is needed the way IPv4 needs `ip=dhcp`.
CONFIG_VIRTIO_NET=enable
CONFIG_IPV6=enable
