# Applied when `[network].mode = "none"`. Explicitly disables the NIC driver and IP stack
# rather than just omitting ipv4.config/ipv6.config, so this holds even if kvm_guest.config's
# own defaults would otherwise turn either on.
CONFIG_VIRTIO_NET=disable
CONFIG_INET=disable
CONFIG_IPV6=disable
