# Applied when `[network].mode` is "ipv4" or "dual" (env: CARGO_UNIKERNEL_NET_IPV4=1) — see
# build_kernel.sh. virtio-net plus the kernel's built-in IPv4 autoconfig (kernel cmdline
# `ip=dhcp`, see pipeline::docker::cmdline).
CONFIG_VIRTIO_NET=enable
CONFIG_IP_PNP=enable
CONFIG_IP_PNP_DHCP=enable
